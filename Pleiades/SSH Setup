SSH key is different for SFE and PFE/Lou

SFE

Add SSH key
ssh tarshad@sfe8.nas.nasa.gov ssh-key-init --set < ~/.ssh/id_rsa.pub

IN LOGGED IN STATE
-------------------
Remove all SSH keys
ssh-key-init --remove=ssh-rsa
Check all SSH keys
ssh-key-init --get


PFE/LOU
Add SSH Key
ssh-copy-id pfe
Remove
Just delete the .ssh/authorized_keys

